export {
  DEFAULT_GAS,
  DEFAULT_GAS_PRICE,
  DEFAULT_NETWORK,
  DEFAULT_NETWORK_CONFIG,
  MAGICVALUE,
  NETWORKS,
  PRIVATE_KEY,
  UP_CONNECTED_ADDRESS,
} from './config';
export type { NetworkInfo, NetworkType } from './network';
